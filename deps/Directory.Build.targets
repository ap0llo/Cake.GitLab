<!-- Prevent the settings from the parent repo to apply to submodules -->
<Project>

    <PropertyGroup>
        <!-- 
            Disable NuGet packaging for submodules.
            Note: This only works because NGitLab does not have a Directory.Build.targets of its own.
        -->
        <IsPackable>false</IsPackable>
    </PropertyGroup>
    
</Project>